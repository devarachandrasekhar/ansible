- name: practice conditions
  hosts: localhost
  
  vars:
    NAME: "devops"

  tasks:
  - name: printing values  when condition matchedss
    ansible.builtin.debug:
      msg: "{{NAME}}"
    when: NAME == "devops"    