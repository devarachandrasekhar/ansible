- name: refering the varibles form inventory file 
  hosts: mongodb

  tasks:
  - name: inventory varibles
    ansible.builtin.debug:
      msg: "mongodb username: {{USERNAME}} , password: {{PASSWORD}}"
