- name: practice loops
  hosts: localhost

  tasks:
  - name: print the loop values
    ansible.builtin.debug:
      msg: "skills: {{item}}"
    loop:
    - aws
    - jenkins
    - docker
    - k8    